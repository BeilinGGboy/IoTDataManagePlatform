# 复制为 .env 并填写实际值
# cp .env.example .env

PORT=8080

# MySQL 配置（不配置则使用内存模式，认证功能不可用）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的密码
DB_NAME=iot_watch_db

# JWT 签名密钥（生产环境务必设置强随机字符串）
# JWT_SECRET=your-random-secret-at-least-32-chars
